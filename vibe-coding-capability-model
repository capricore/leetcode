Vibe Coding 能力模型梳理
1. 什么是「Vibe Coding」
在有 AI/agent、脚手架和各种工具的环境下，一个工程师能否快速、成体系地把一个 idea 变成一个「能跑的产品/功能」，并且过程中不混乱。

它不是单一技术栈，而是下面几部分能力的综合：

问题拆解与建模  
工具 & AI 使用能力（包括 skill、MCP、agent 等）  
技术直觉 & 架构感  
前后端基本功  
调试 & 迭代节奏  
工程实践意识  

2. 工具 & AI 协作能力（含 skill / MCP / agent）
2.1 Skill 概念与使用能力
这里的 skill 可以理解为「让 AI/agent 完成特定操作的能力模块」，比如：

- 调用某个 HTTP API
- 读写数据库
- 执行本地命令 / 运行脚本
- 根据模板生成代码文件

需要具备的要点：

（1）理解 skill 的本质  
- Skill 是「对外暴露的一组操作」，而不是业务逻辑本身。  
- Skill 是让模型能够「安全、结构化」地做事情的接口。  

（2）设计 skill 接口的能力  
- 输入定义清晰：  
  - 参数有哪些，类型是什么？  
  - 哪些是必填，哪些是可选？  
- 输出结构明确：  
  - 结果格式统一（例如 { success, data, error }）  
  - 出错时返回什么？（错误码、错误信息）  
- 清晰的能力边界：  
  - 能做什么，不该做什么（例如只读不写、不能删除等）。  

（3）教 AI 正确使用 skill 的能力  
- 为 skill 写清楚的描述（description），而不是一句模糊描述。  
- 用自然语言/系统提示告诉模型：  
  - 在什么场景下调用这个 skill  
  - 调用频率/时机（例如「只在用户明确要求时调用」）  
  - 参数含义和约束  

可面试考察方式：

给一个现有的后端 API（比如 Todo CRUD），让候选人：

- 设计要暴露给 AI 的 skill 列表；  
- 为每个 skill 定义输入参数、输出结构；  
- 写一小段说明：模型应该什么时候使用这个 skill。  

2.2 MCP（Model Context Protocol）相关能力
MCP 是一种让模型安全、统一地访问外部工具/数据源的协议。核心是：把外界能力（文件、数据库、服务等）「标准化」给模型使用。

关键能力点：

（1）理解「模型与外界交互」的抽象  
- 模型本身是无 IO 的，需要通过工具接口访问外界。  
- MCP 这类协议就是统一定义这些接口：  
  - 如何声明有哪些资源  
  - 如何定义可以执行的操作  
  - 如何描述参数和返回  

（2）理解 MCP 式能力暴露的元素  
- Provider / Server：对模型提供能力的后端（文件系统、API 网关、数据库等）。  
- Resource / Tool / Action：  
  - Resource：可浏览/读取的数据源，例如某个目录、某张表。  
  - Action / Tool：可执行的操作，例如：查询、写入、调用第三方 API。  
- Schema 描述：  
  - 使用 JSON Schema 等对参数和结果进行结构化定义。  
  - 模型可以据此自动构造调用请求。  

（3）设计一个 MCP 风格集成的能力  
示例：让 agent 访问公司「订单服务」。

- 定义资源：  
  - `orders`：支持分页查询、按状态过滤。  
- 定义 actions：  
  - `getOrderById`  
  - `listOrdersByUser`  
- 定义参数结构：  
  - `getOrderById` 的 id 字段类型、必填  
  - `listOrdersByUser` 的 userId、status、分页参数  
- 安全边界：  
  - 不提供删除订单、修改金额的 action  
  - 对敏感字段（如用户手机号）做脱敏或不返回  

可面试考察方式：

问：如果你要让一个 agent 可以查询你们系统里的订单列表，你会：

- 暴露哪些 action？  
- 参数与返回怎么设计？  
- 有哪些安全/权限限制考虑？  

2.3 多工具 / 多 Agent 协调能力
优秀的 AI 时代工程师要理解：

一个 agent 不应该「什么都自己干」，而是：

- 主 agent：理解需求、决策、拆任务。  
- 工具/子 agent：执行具体子任务（改代码、查文档、操作 CI/CD 等）。  

能力点：

（1）理解角色分工  
不同 agent/工具负责不同领域：

- 代码修改 / Refactor  
- 文档搜索 / 知识库查询  
- 部署 / 运维操作  
- 数据查询 / 报表生成  

（2）能设计简单的编排流程  
例如「从 0 到 1 搭一个小服务」：

1. 需求解析（主 agent）  
2. 选框架 + 初始化项目（工具：脚手架/命令执行）  
3. 定义接口与数据结构（主 agent）  
4. 生成后端代码（代码 agent + skill）  
5. 生成前端页面（前端代码 agent）  
6. 部署到测试环境（DevOps agent / MCP 工具）  

（3）知道什么时候拆子 agent  
- 当子任务需要不同上下文或权限时（例如生产环境部署权限）。  
- 当子任务需要不同工具组合时（一个负责数据库，一个负责 Git）。  

可面试考察方式：

问：如果你让 AI 帮你全流程搭一个简单的 CRUD 应用，你会：

- 设计哪些子 agent 或工具？  
- 这些 agent 分别负责什么？  
- 大概的调用/编排顺序是怎样的？  

3. 问题拆解与建模能力
Vibe coding 的核心：能快速从模糊需求拆出可实现的结构。

3.1 从需求到结构的拆解
能力点：

能快速识别：

- 页面/路由：有哪些页面？大致布局？  
- 数据结构：涉及哪些实体？字段？关系？  
- API：前端需要哪些接口？参数和返回结构？  
- 状态流转：用户操作路径是什么？  

例：需求

“做一个简单的投票页面”

合理拆解：

- 数据模型：  
  - Poll：id, question, options[]  
  - Option：id, text, votes  
- 后端接口：  
  - `GET /poll` → 返回当前问题 + 选项 + 票数  
  - `POST /poll/vote` → body: optionId  
- 前端：  
  - 页面展示：问题 + 选项 + 当前票数  
  - 用户点击某个按钮 → 调 `POST /poll/vote` → 再调 `GET /poll` 刷新  

3.2 「先打通主路径」的意识
先完成最小可运行链路：

- 后端接口 → 正确返回数据  
- 前端页面 → 能连上接口并显示数据  

UI 细节、边缘情况、复杂优化可以放在后面。

面试观察点：

候选人是否：

- 一开始就纠结样式/复杂架构？  
- 还是先把「前端调用后端，数据能跑起来」打通？  

4. 技术直觉 & 架构感（轻量版）
不要求复杂架构设计，但要有「不乱」的基本感。

4.1 文件 / 模块组织
前端：

至少做到：

- 页面组件 vs 纯 UI 子组件 分开。  
- 接口请求逻辑放在单独模块（如 `api.ts`），而不是散落在各组件。  

后端：

简单分层：

- 路由 / 控制器：接收请求，做基本验证。  
- service/logic：执行业务逻辑。  
- data layer：数据库或存储操作。  

观察点：是否倾向于「所有逻辑堆在一个文件/函数里」。

4.2 API 设计直觉
- 资源命名清晰：  
  - 用 `/todos`, `/users` 比 `/doTodoAction` 这种好。  
- 参数与返回结构：  
  - 返回统一结构（例如 `{ data, error }` 或 `{ success, data }`）。  
  - 合理使用 HTTP 状态码（200、400、401、404、500 等）。  
  - 错误信息对前端/调用者友好。  

5. 前后端基本功（底座）
AI/agent 可以帮写代码，但工程师需要有基本的“辨别能力”。

5.1 前端基本功
以 React/Next/Vue 为例：

- 组件 & 状态：  
  - 基本站位：函数组件、props、state。  
  - 列表渲染、条件渲染。  
- Hooks/响应式：  
  - 基本的生命周期（如 React 的 `useEffect`）。  
  - 理解依赖变更对渲染的影响。  
- 网络请求：  
  - 使用 fetch 或 axios 调接口。  
  - 处理 loading 状态、错误状态。  
- 表单：  
  - 输入框绑定状态。  
  - 提交时收集表单数据。  
  - 简单校验（非空、格式）。  

5.2 后端基本功（以 Node/Express/Nest 为例）
- 路由：  
  - GET/POST/PATCH/DELETE。  
  - Path 参数、Query 参数、Body 的区别和使用。  
- 数据存储：  
  - 内存结构模拟（数组、对象）。  
  - 有数据库时：会写简单 CRUD（SQL 或 ORM）。  
- 错误处理：  
  - 用 try/catch 或错误中间件处理异常。  
  - 返回合理的 status code 和错误信息。  

6. 调试 & 迭代节奏
6.1 调试方法
前端：

- Console：查看错误信息、打印变量。  
- Network：  
  - 检查请求是否发送。  
  - 返回的 status code 和 response 内容是否正确。  

后端：

- 日志：  
  - 打印请求参数、关键中间结果。  
  - 出错时查看错误堆栈。  
- 工具验证：  
  - 用 Postman / curl / 浏览器测试接口是否工作正常。  

6.2 小步迭代 & 快速验证
- 写少量代码 → 立刻运行 → 验证 → 再继续。  
- 避免长时间「关门造车」不运行，最后积累一大堆问题。  

面试时可故意设置一个小坑（如前后端参数名不匹配），观察：

候选人如何排查：

- 是直接问 AI ？  
- 还是看网络请求、日志，然后有系统化排查？  

7. 工程实践意识（轻量）
即使在 20 分钟的小题中，也能观察到一些习惯：

- 错误处理：  
  - 前端：请求失败时给用户提示（alert、toast）。  
  - 后端：不要直接崩溃；返回错误码和信息。  
- 输入校验：  
  - 简单非空校验，避免后端收到空字符串等非法参数。  
- 可扩展性：  
  - 数据结构预留字段，而不是全写死在代码里。  
- 配置意识：  
  - 在真实项目里会用环境变量存放敏感配置（DB 连接、API Key），不会写死在代码中。  
  - 即使在面试里没时间实现，能主动提到这一点，也加分。  

8. 面试使用建议与观察点
8.1 实战题 + 过程观察
- 设计一个 20 分钟左右的全栈小功能（例如 Todo、留言板、投票等）。  
- 允许使用 AI/agent、脚手架等工具。  

面试官重点看：

- 需求拆解顺序  
- 文件/模块组织  
- 调试方式  
- 与 AI 的交互方式（提示是否清晰、是否 review AI 输出）  

8.2 Skill/MCP 思维的问答
可以加一段“设计题”：

- 问：如果要把刚刚实现的功能暴露给一个 AI agent 使用，你会设计哪些 skill？  
- 问：如果用 MCP 这类机制，让模型在受控条件下访问数据，你会怎么暴露资源和 action？  
- 问：你会如何限制 agent 的权限，避免破坏数据或泄露隐私？  

8.3 API / 数据建模追问
让候选人解释自己的接口设计：

- 为什么路径这么定义？  
- body 和 query 的选择依据？  

追问：“如果以后要加权限、分页、筛选，你会怎么改？”

9. 总结
「Vibe coding」能力，不是某个框架的熟练度，而是：

- 能把模糊需求快速拆成数据、接口和 UI 结构；  
- 能高效、安全地使用 AI/agent、skill、MCP 等工具；  
- 有基本的技术直觉和模块化思维，不把项目写成一团；  
- 具备足够的前后端基础，使得 AI 生成的代码能被辨别和驾驭；  
- 能够快速调试和迭代，保持小步验证；  
- 在时间有限的任务中，仍然具备基本工程意识。  

将这些能力点拆成可观察行为和问题，可以形成一套完整的「vibe coding」面试考察体系。

---

PrivateGPT can make mistakes. Check important info.
