下面这些题目都控制在：**有 AI/agent + 熟练工程师 ≈ 20 分钟能做出“能跑的版本”**。你可以按候选人水平选简单/稍难一些的。

---

## 一类：极简 CRUD / 列表 + 表单

### 1. 迷你待办（Todo）列表（无持久化版）

**要求（20 分钟版）：**

- 后端：
  - 一个内存数组保存 todos：`{ id, text, done }`
  - 提供三个接口：
    - `GET /todos`：返回全部
    - `POST /todos`：新增（body 传 `text`）
    - `PATCH /todos/:id/toggle`：切换 done 状态
- 前端：
  - 显示 todos 列表（done 的用划线/灰色表示）
  - 顶部有一个输入框 + 按钮，能新增 todo
  - 每条 todo 有一个按钮可以切换完成状态（调 `toggle` 接口）

**考察点：**

- 基本前后端打通
- 简单状态管理、列表渲染、接口调用
- 能否快速利用脚手架 + AI 生成样板

---

### 2. 简单留言板（Message Board）

**要求：**

- 后端：
  - 内存数组 `{ id, author, content, createdAt }`
  - `GET /messages`：按时间倒序返回
  - `POST /messages`：创建新留言（body: `author`, `content`）
- 前端：
  - 显示留言列表（展示作者、内容、时间）
  - 底部一个表单：输入昵称 + 内容，提交后刷新列表
  - 简单错误提示：请求失败时弹一个 alert / message

**考察点：**

- 基本表单处理
- 提交后刷新列表（重新拉接口或本地更新）
- 前端错误处理意识

---

### 3. 简易计数器后台 + 前端展示

**要求：**

- 后端：
  - 内存中维护一个数字 `count`
  - `GET /count`：返回当前值
  - `POST /count/increment`：`count++`
  - `POST /count/decrement`：`count--`
- 前端：
  - 展示当前 count（页面加载时从 `/count` 拉）
  - 两个按钮：+1 / -1，对应后端接口
  - 操作后刷新显示

**考察点：**

- 前端/后端请求交互流程是否顺畅
- 组件状态更新、节奏感

---

## 二类：带一点业务逻辑的小功能

### 4. 简易投票系统（单问题投票）

**要求：**

- 后端：
  - 固定一个投票问题，比如：“你最常用的前端框架？”
  - 备选项如：`React`, `Vue`, `Angular`, `Svelte`
  - 内存结构：`{ option: string, votes: number }[]`
  - 接口：
    - `GET /poll`：返回各选项当前票数
    - `POST /poll/vote`：body 传 `option`，对应选项 votes +1
    - 简单校验：如果 option 不在预设列表，返回 400
- 前端：
  - 显示问题 + 每个选项
  - 每个选项旁有“投票”按钮，点击后请求 `vote` 接口
  - 下方展示当前票数（可用简单柱状视觉，比如用长度不同的 div）

**考察点：**

- 简单业务校验（无效 option）
- 一次交互多次刷新数据的节奏（投票后刷新）

---

### 5. 价格计算器（含后端规则）

**情景：** 一个“购物车总价计算器”，价格计算在后端。

**要求：**

- 后端：
  - 固定几种商品：`[{ id: 'apple', price: 3 }, { id: 'banana', price: 2 }, ...]`
  - 接口：`POST /cart/total`
    - 入参：`items: [{ id, quantity }]`
    - 返回：`{ total }`
  - 规则示例（可直接写死）：
    - 如果总数量 ≥ 5，打 9 折（`total * 0.9`）
- 前端：
  - 一个简单表单：
    - 数量输入：apple 数量、banana 数量……
  - 点击“计算总价”按钮，调用接口，显示总价

**考察点：**

- 在后端写一些简单业务逻辑而不把逻辑全部放前端
- 参数格式、接口设计

---

## 三类：稍带点“状态/过滤”的界面

### 6. 简单过滤列表（服务器端过滤）

**要求：**

- 后端：
  - 内存列表，比如用户列表：`{ id, name, role }`
  - `GET /users?role=admin|user`：若传 role，则按角色过滤
- 前端：
  - 下拉框选择 role（全部 / admin / user）
  - 根据选中的 role 调后端 `/users` 并展示列表
  - 切换筛选项时重新请求

**考察点：**

- Query 参数使用
- 前端对过滤条件和数据的联动

---

### 7. 迷你登录表单（伪鉴权，无 token）

**要求：**

- 后端：
  - 写死一个用户：`username: admin, password: 123456`
  - `POST /login`：body 传 `username`, `password`
    - 正确则返回 `{ success: true, username }`
    - 错误则返回 401 + `{ success: false, message }`
- 前端：
  - 登录表单（username + password）
  - 提交后根据结果：
    - 成功：显示“欢迎，xxx”
    - 失败：显示错误信息

**考察点：**

- 基础安全意识（不在前端硬编码密码校验）
- 基本错误处理

---

## 四类：偏“体验/节奏感”的小题

### 8. 简单搜索建议（前后端版）

**要求：**

- 后端：
  - 固定一个字符串数组：一堆城市名或者人名
  - `GET /search?q=xxx`：返回 name 中包含 `q` 的前 5 个
- 前端：
  - 顶部一个输入框，用户输入时（`onChange`）触发请求
  - 在输入框下方显示建议列表
  - 为避免频繁请求，可让候选人：
    - 直接每次输入都调用（不强制 debounce）
    - 或让他自己提：可以加 debounce（看他的工程意识）

**考察点：**

- 实时请求/更新 UI 的节奏
- 对简单 UX 的感知（输入变化时行为）

---

### 9. 简单“点赞”按钮（本地 + 后端同步）

**要求：**

- 后端：
  - 内存中保存一个 `likes` 计数
  - `GET /likes`：获取总数
  - `POST /likes`：总数 +1，返回新的总数
- 前端：
  - 一个“点赞”按钮，旁边显示总数
  - 页面加载时先拉 `/likes`
  - 点击时立刻在前端先 +1（乐观更新），同时请求后端
  - 如果请求失败，回滚到原来的值并提示错误

**考察点：**

- 乐观更新的思路
- 请求失败时的回滚和用户反馈

---

## 五类：稍偏“vibe”的题目（结构 + 沟通）

### 10. 简单前后端“健康监控面板”

**要求：**

- 后端：
  - `GET /status`：返回：
    ```json
    {
      "app": "ok",
      "db": "ok",
      "cache": "down"
    }
    ```
    （直接写死）
- 前端：
  - 页面显示三个模块：App / DB / Cache
  - 不同状态使用不同颜色/标记（ok 绿色，down 红色）
  - 提供一个“刷新状态”按钮，重新请求 `/status`

**考察点：**

- 如何把一个简单 JSON 展示为较清晰的 UI
- 组件拆分：是否会写一个小 `StatusCard` 组件，而不是堆在一起

---

## 使用建议

1. **时间控制**
   - 题目本身控制在：熟练 + AI 情况下 10–15 分钟能出“可运行版本”。
   - 面试中可以给 20 分钟，其中包括：
     - 15 分钟实现
     - 5 分钟让候选人讲解代码和思路

2. **搭配方式**
   - 对初中级：选 1 个简单 CRUD（如 Todo / Message Board）+ 1 个小逻辑题（投票/登录）。
   - 对中高级：给一个 CRUD + 要求“尽量展示你的工程习惯（结构、错误处理、封装）”。

3. **观察重点**
   - 如何拆解：先从后端开始还是前端原型先行？
   - 如何用 AI：提示是否清晰、会不会对 AI 输出做 review。
   - Bug 时的行为：Console / Network / 后端日志用得如何。

如果你告诉我你团队当前的技术栈（例如：Next.js + NestJS / React + Node/Express / Vue + Spring Boot 等），我可以基于其中一个栈，为上面某个题目写一版更具体的“面试官操作说明 + 期望候选人产出”。
